name: Deploy

on: workflow_dispatch

env:
  CI: false

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - run: pnpm install
      - run: pnpm build
      - run: pm2 restart forms
